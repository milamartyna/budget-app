## Connect to db
To create local instance of postgres:
 - have docker running
 - run ``docker-compose up -d``

## Access Swagger UI
After running application locally swagger go to:
http://localhost:8080/swagger-ui/index.html
