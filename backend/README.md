## To run locally
### Connect to db
To create local instance of postgres:
 - have docker running
 - run ``docker-compose up postgres``
### Start application
- run ``gradle bootRun``

## Access Swagger UI
After running application, to access swagger go to:
http://localhost:8080/swagger-ui/index.html
